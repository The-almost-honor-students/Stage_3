version: "3.8"

services:
  nginx:
    image: nginx:trixie-perl
    container_name: search_nginx
    ports:
      - "8000:8000"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - search_net

networks:
  search_net:
    driver: bridge
